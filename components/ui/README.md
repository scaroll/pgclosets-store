# UI Components

This directory contains shadcn/ui components and other reusable UI primitives.

## Purpose
- Buttons, inputs, dialogs, dropdowns
- Toast notifications, modals, popovers
- Cards, badges, avatars
- Other atomic UI components

## Usage
Components here should be highly reusable and framework-agnostic where possible.
