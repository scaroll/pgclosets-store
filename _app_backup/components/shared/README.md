# Shared Components

This directory contains shared business logic components.

## Purpose
- Custom buttons with specific styling
- Form components
- Modal dialogs
- Loading states
- Error boundaries
- Other shared components that don't fit in UI

## Usage
Components here are reusable across multiple features but may contain business logic.
