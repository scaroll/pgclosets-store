import { redirect } from 'next/navigation';

// Placeholder: Hardware collection not yet available
// Redirect to main products page
export default function HardwarePage() {
  redirect('/products');
}